spring.application.name=sdfcc-backend-test
spring.datasource.url=jdbc:h2:mem:sdfcc-test;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.H2Dialect
### Spring Security
#jwt.issuer=${JWT_ISSUER:sdfcc-backend}
#jwt.expiration-hours=${JWT_EXPIRATION_HOURS:1}
#jwt.signing-key=${JWT_SIGNING_KEY:REPLACE_ME_WITH_A_STRONG_KEY}
