# Non-production values used only to lint and render the chart in CI.
config:
  JWT_REQUIRE_CONFIGURED_KEYS: "false"

secret:
  enabled: true
  data:
    SPRING_DATASOURCE_URL: jdbc:postgresql://postgresql:5432/sdfcc
    SPRING_DATASOURCE_USERNAME: chart-validation
    SPRING_DATASOURCE_PASSWORD: chart-validation-only
