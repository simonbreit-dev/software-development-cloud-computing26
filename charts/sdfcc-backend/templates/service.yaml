apiVersion: v1
kind: Service
metadata:
  name: {{ include "sdfcc-backend.fullname" . }}
  labels:
    {{- include "sdfcc-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
      {{- if and .Values.service.nodePort (or (eq .Values.service.type "NodePort") (eq .Values.service.type "LoadBalancer")) }}
      nodePort: {{ .Values.service.nodePort }}
      {{- end }}
  selector:
    {{- include "sdfcc-backend.selectorLabels" . | nindent 4 }}
